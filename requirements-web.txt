# Runtime deps for the STATIC web deploy only (see Dockerfile).
# The heavy pipeline (torch, chromadb, sentence-transformers, pandas, …) is NOT
# needed at runtime — reports are built offline via `make delta-batch` and baked
# into the image. Full dev deps live in requirements.txt.
fastapi==0.116.1
uvicorn==0.35.0
jinja2==3.1.6
python-dotenv==1.2.2
